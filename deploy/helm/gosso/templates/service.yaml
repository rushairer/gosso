apiVersion: v1
kind: Service
metadata:
  name: {{ include "gosso.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "gosso.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gosso.selectorLabels" . | nindent 4 }}
