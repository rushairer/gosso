apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gosso.fullname" . }}-test-connection"
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "gosso.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  restartPolicy: Never
  containers:
    - name: wget
      image: busybox:1.36
      command: ['wget']
      args: ['--timeout=10', '{{ include "gosso.fullname" . }}:{{ .Values.service.port }}/health']
